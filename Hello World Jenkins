pipeline {
    agent any
    stages {
        stage('stage 1') {
            steps {
                echo 'Hello world' 
                echo BUILD_ID #Print_build_ID
            }
        }
    
    }
}
